<%= form_for(current_user.relationships.build(teacher_id: @user.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :teacher_id %></div>
  <%= f.submit "Like", class: "btn btn-large btn-primary" %>
<% end %>